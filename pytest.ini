[pytest]
markers =
    network: tests that require network connectivity to run
testpaths = test_recipe.py scripts
 
# Skip tests marked as network by default (can be overridden with env vars)
addopts = -v 