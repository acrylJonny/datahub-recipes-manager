from django.db import migrations


class Migration(migrations.Migration):
    dependencies = [
        ('metadata_manager', '0001_initial'),
    ]

    operations = [
        # These operations have been applied manually directly to the database
        # using SQLite commands, so we're leaving this migration file empty
        # to avoid "duplicate column" errors.
    ] 